/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FileInputAccessorDirective } from './file-input-accessor.directive';

describe('Directive: FileInputAccessor', () => {
  it('should create an instance', () => {
    const directive = new FileInputAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
